<Page
    x:Class="FlexPanelGallery.Pages.JustifyContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:flex="using:Duct.Flex">

    <ScrollViewer>
        <StackPanel Spacing="16" Padding="24">
            <TextBlock Text="Justify Content" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Controls how items are distributed along the main axis." TextWrapping="Wrap" />

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <ComboBox x:Name="JustifyCombo" Header="JustifyContent" SelectedIndex="0"
                              SelectionChanged="JustifyCombo_SelectionChanged">
                        <ComboBoxItem Content="FlexStart" Tag="FlexStart" />
                        <ComboBoxItem Content="Center" Tag="Center" />
                        <ComboBoxItem Content="FlexEnd" Tag="FlexEnd" />
                        <ComboBoxItem Content="SpaceBetween" Tag="SpaceBetween" />
                        <ComboBoxItem Content="SpaceAround" Tag="SpaceAround" />
                        <ComboBoxItem Content="SpaceEvenly" Tag="SpaceEvenly" />
                    </ComboBox>

                    <Border BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1"
                            MinHeight="100">
                        <flex:FlexPanel x:Name="DemoPanel" Direction="Row" JustifyContent="FlexStart">
                            <Border Width="60" Height="60" Background="CornflowerBlue" CornerRadius="4" />
                            <Border Width="80" Height="60" Background="MediumSeaGreen" CornerRadius="4" />
                            <Border Width="50" Height="60" Background="Coral" CornerRadius="4" />
                        </flex:FlexPanel>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
