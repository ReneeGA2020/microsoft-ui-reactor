<Window
    x:Class="WebView2Test.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Title="WebView2 Test">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText" Text="Loading Monaco..." FontSize="16" Padding="16" />
        <!-- Use our MonacoEditor control directly in XAML, no Duct reconciler -->
    </Grid>

</Window>
